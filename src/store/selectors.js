export const ingredientsSelector = state => state.ingredients;

export const burgerConstructorSelector = state => state.burgerConstructor;

export const ingredientDetailSelector = state => state.ingredientDetail;

export const orderSelector = state => state.order;